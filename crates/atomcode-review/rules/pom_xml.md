[Maven pom.xml] This batch contains Maven POM; pay extra attention to:
- Newly added code must not contain snapshot version qualifiers; other versions are acceptable.
  Note: when a version is not declared in code it is usually managed by a parent POM; when the version number is not on a newly added line, ignore this rule.
